# SVM Experiments

- SVM serves as a lightweight baseline to validate the pipeline before heavier models.
- Limitations: flattened pixels are high-dimensional and shallow; struggles with complex textures; limited feature learning compared to CNNs.
- Next: move to a small custom CNN to learn richer representations.
